{
  "name": "resume",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist/static && vite build --ssr src/server-render.tsx --outDir dist/server && node prerender",
    "prebuild": "rm -rf dist/static",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.33",
    "@types/react-dom": "^17.0.10",
    "@vitejs/plugin-react": "^1.0.7",
    "vite-plugin-compression": "^0.5.1",
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  }
}
